---
name: Trigger deployment

on:
  workflow_run:
    workflows: ["Docker"]
    branches: ["master"]
    types: ["completed"]

jobs:
  prod:
    runs-on: ubuntu-latest
    environment: Production
    permissions:
      deployments: write
    steps:
      - name: Deploy
        run: echo "Triggering deployment..."
